/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['api-assets.clashofclans.com'], // Add the Clash of Clans domain here
      },
}

module.exports = nextConfig
